# Sources for memory-infra-neighbours-2026-09

Research-agent web sweep, 2026-09-24. F = fetched through WebFetch's summariser, S = search snippet only; neither is a verbatim read.
Independently verified by the author: modernc.org/sqlite/vec (pkg.go.dev), go-fsrs README, gonum graph/community docs, Kùzu archive notice, GoodMemory README, sqlite-vec README (pre-v1 notice; no statement on brute-force vs index). NOT found on GitHub: "projectmem".

# Web research: memory-infra neighbours for ghost (2026-09-24)

Legend: F = fetched primary source (README/docs/API), S = search snippet only. UNVERIFIED = could not confirm.
GitHub metadata via api.github.com (stars, licence, pushed_at, archived, fork).

## A. LLM-free / LLM-optional agent memory stores

## B. Embedded databases as memory infra

## C. Deterministic memory algorithms as libraries

## D. Memory-as-infra positioning

## (1) Closes a ghost gap

## (2) Tempting but off-thesis

## Raw log (appended as fetched)

### Batch 1 (B: SQLite ecosystem, Dolt, Kuzu, Cozo, sync)
- ncruces/go-sqlite3 [F api+README] 1,110 stars, MIT, Go, pushed 2026-09-23. QUOTE: "Go module `github.com/ncruces/go-sqlite3` is a `cgo`-free SQLite wrapper." README lists "encryption at rest" -> vfs/adiantum. https://github.com/ncruces/go-sqlite3
- ncruces vfs/adiantum [F pkg.go.dev] QUOTE: "This package wraps an SQLite VFS to offer encryption at rest." QUOTE: "The 'adiantum' VFS wraps the default SQLite VFS using the Adiantum tweakable, length-preserving encryption." Keys via URI params key/hexkey/textkey or PRAGMA; Argon2id for textkey. Caveat QUOTE: "This package does not claim protect databases against tampering or forgery." Deterministic (snapshot diffing reveals changed blocks). https://pkg.go.dev/github.com/ncruces/go-sqlite3/vfs/adiantum
- asg017/sqlite-vec [F api+README] 8,133 stars, Apache-2.0/MIT, C, pushed 2026-05-18, not archived. QUOTE: "Written in pure C, no dependencies, runs anywhere SQLite runs"; QUOTE: "`sqlite-vec` is a pre-v1, so expect breaking changes!"; Go bindings `github.com/asg017/sqlite-vec/bindings/go`. vec0 virtual tables float/int8/binary. https://github.com/asg017/sqlite-vec
- dolthub/dolt [F api] 24,506 stars, Apache-2.0, Go, pushed 2026-09-24. dolthub/driver [F README] QUOTE: "This package provides a `database/sql` compatible driver for embedding Dolt inside a Go application." QUOTE: "It allows you to access local Dolt databases via the file system, akin to SQLite, without running a Dolt server process." CGo: NOT MENTIONED on driver page (Dolt itself is Go). https://github.com/dolthub/driver
- kuzudb/kuzu [F api] 4,025 stars, MIT, C++, pushed 2025-10-10, **archived: true**. => DEAD upstream (off-thesis). https://github.com/kuzudb/kuzu
- cozodb/cozo [F api+README] 4,123 stars, MPL-2.0, Rust, pushed 2024-12-04 (no commits ~21 months). QUOTE: "You can now create HNSW (hierarchical navigable small world) indices on relations containing vectors." QUOTE: "Time travel in the database setting means tracking changes to data over time and allowing queries to be logically executed at a point in time" QUOTE: "For the languages officially supported, only Golang wraps the C API directly." (=> CGo). Backends: mem, SQLite, RocksDB, Sled, TiKV. https://github.com/cozodb/cozo
- benbjohnson/litestream [F api] 14,393 stars, Apache-2.0, Go, pushed 2026-09-21. "Streaming replication for SQLite." https://github.com/benbjohnson/litestream
- vlcn-io/cr-sqlite [F api] 3,800 stars, MIT, Rust, pushed 2026-08-10. "Convergent, Replicated SQLite. Multi-writer and CRDT support" (loadable C/Rust extension => CGo or wasm). https://github.com/vlcn-io/cr-sqlite

### Batch 2 (B continued, C start, A discovery)
- ncruces ext/README [F]: bundled extensions: array, blobio, bloom, closure, csv, fileio, fts5, hash, ipaddr, lines, pivot, regexp, rtree, serdes, spellfix1, statement, stats, unicode, uuid, vec1 ("approximate nearest-neighbor vector search"). sqlite-vec itself NOT in default bundle. https://github.com/ncruces/go-sqlite3/blob/main/ext/README.md
- asg017/sqlite-vec-go-bindings [F README]: QUOTE: "If you are using the ncruces/go-sqlite3 library, then use the `ncruces` portion" ... QUOTE: "will automatically register a new SQLite WASM build that includes `sqlite-vec` functions by default". 45 stars, Apache-2.0/MIT. => sqlite-vec IS reachable CGo-free via ncruces wasm. https://github.com/asg017/sqlite-vec-go-bindings
- XTDB docs [F]: QUOTE: "XTDB builds upon the 'bitemporal' capabilities defined within SQL:2011 but makes those capabilities both ubiquitous and automatic"; QUOTE: "XTDB is free-to-use and open source under the MPL license"; deploy via Docker/JVM; Go listed among 12 client drivers (server, not embedded). https://docs.xtdb.com/
- marcboeker/go-duckdb [F README]: CGo required; QUOTE: "Statically linking DuckDB increases your binary size." MIT. No pure-Go build. https://github.com/marcboeker/go-duckdb
- tursodatabase/turso [F README]: QUOTE: "Vector support support including exact search and vector manipulation"; QUOTE: "Encryption at rest for protecting the data locally." (experimental); QUOTE: "compatible with SQLite at the SQL dialect, file format, and C API levels, and existing SQLite database files work as-is"; Go: `go get turso.tech/database/tursogo` (CGo status NOT MENTIONED on README); "we have not yet reached 1.0"; MIT. https://github.com/tursodatabase/turso
- lancedb/lancedb [F README]: Lance columnar format; QUOTE: "Zero-copy, automatic versioning, manage versions of your data without needing extra infrastructure."; SDKs Python/Node/Rust/REST — NO Go SDK; Apache-2.0. https://github.com/lancedb/lancedb
- open-spaced-repetition/go-fsrs [F api+README]: 146 stars, MIT, Go, pushed 2026-07-25, not fork. QUOTE: "A Go library for building spaced-repetition systems with FSRS."; FSRS v6, QUOTE: "FSRS v6 uses a 21-element weight vector (Weights = [21]float64)."; Card lifecycle New→Learning→Review→Relearning; Repeat(card, now)/Next(card, now, grade); optimizer package "in_development". https://github.com/open-spaced-repetition/go-fsrs
- gonum/graph/community [F pkg.go.dev]: QUOTE: "Modularize returns the hierarchical modularization of g at the given resolution using the Louvain algorithm." BSD-3-Clause. No Leiden. https://pkg.go.dev/gonum.org/v1/gonum/graph/community
- Leiden in Go [S]: k8nstantin/go-leiden ("pure-Go ... zero external dependencies ... port of graspologic-native"), vsuryav/leiden-go, FlavioCFOliveira/GoGraph. To verify. https://pkg.go.dev/github.com/k8nstantin/go-leiden
- A discovery [S]: hjqcan/goodmemory ("Local-first, auditable memory layer ... durable SQLite, embedding-free recall, MCP access, and opt-in governed writeback"; Bun/TS); hermes-labs-ai/fidelis ("zero-LLM agent memory ... BM25, dense vectors, and reciprocal rank fusion with no LLM call ... returns your original passages verbatim"; Python, MIT); basicmachines-co/basic-memory (Markdown files + SQLite index, MCP); GibsonAI/memori (SQL-native; uses LLM for entity extraction -> likely off-thesis, verify); CaviraOSS/LongMemory ("deterministic decay, explicit reinforcement, consolidation"); komakeule57-maker/mcp-memory ("plain SQLite with FTS5 ... No vector store, no embeddings"); Engram Go binary (SQLite+FTS5, MCP/HTTP/CLI/TUI) — parent repo TBD among many forks.

### Batch 3 (A stores, C libs)
- Dolt querying-history [F docs]: QUOTE: "Dolt SQL supports a variant of SQL 2011 syntax to query non-HEAD revisions of a database via the `AS OF` clause"; QUOTE: "the `dolt_history` tables provide a row for every revision of a row in a table"; dolt_diff/dolt_commit_diff tables; QUOTE: "`AS OF` always names a revision at a specific Dolt commit." https://www.dolthub.com/docs/sql-reference/version-control/querying-history
- CaviraOSS/LongMemory [F api+README]: 4,512 stars, Apache-2.0, TypeScript, pushed 2026-09-20. SQLite storage. QUOTE: "Strict recall applies temporal, contradiction, contract, confidence, and grounding gates. Historical recall preserves superseded truth. Associative recall follows semantic, lexical, entity, activation, and graph signals." QUOTE: "deterministic decay, explicit reinforcement, consolidation, compression, and reconsolidation" (formula NOT MENTIONED). QUOTE: "Your model stays stateless". QUOTE: "Hydrograph memory substrate with immutable nodes, executable edges, worlds, entities, facets, and traces". QUOTE: "Immutable content, provenance, and temporal truth". Surfaces: npm/CLI/HTTP/MCP/dashboard/VS Code. Encryption NOT MENTIONED. https://github.com/CaviraOSS/LongMemory
- hjqcan/goodmemory [F api+README]: 18 stars, MIT, TypeScript(Bun), pushed 2026-09-14. QUOTE: "Default recall is single-pass and rules-only, and default extraction is unchanged; nothing happens unless you opt in." QUOTE: "Raw transcripts are not persisted as memory. Assistant-originated durable memory is blocked unless the host confirms or verifies it." Writeback modes off/observe/review/selective. QUOTE: "The audit ledger stores bounded redacted candidate previews, candidate keys, typed linked record ids, status, reasons, host, mode, timestamps." QUOTE: "`exportMemory()` gives the user an audit/export path. Its `pages/` bundle is a memoryfield-compatible directory of note pages with a SHA-256 manifest." Hooks for Codex/Claude Code via `goodmemory setup`. https://github.com/hjqcan/goodmemory
- hermes-labs-ai/fidelis [F api+README]: 23 stars, MIT, Python, pushed 2026-09-23. QUOTE: "hybrid retrieval path combines keyword search, dense-vector similarity, and reciprocal rank fusion" (embeddings via Ollama nomic-embed-text). QUOTE: "Corrections retain superseded records, and recall supports validity dates and historical views". Six MCP tools incl. fidelis_correct. Decay/graph/export/encryption NOT MENTIONED. LongMemEval-S 470-question run 2026-09-21. https://github.com/hermes-labs-ai/fidelis
- basicmachines-co/basic-memory [F api+README]: 4,032 stars, AGPL-3.0, Python, pushed 2026-09-24. QUOTE: "Just files plus a local SQLite index. No servers required." QUOTE: "Entities have `Observations` (facts about them) and `Relations` (links to other entities)." QUOTE: "Hybrid full-text + vector ranking with FastEmbed embeddings, on SQLite or Postgres." QUOTE: "Optional cross-encoder reranking." `build_context` navigates memory:// URLs. Cloud QUOTE: "$15.00/mo". Version history NOT MENTIONED (git on md files implied). https://github.com/basicmachines-co/basic-memory
- komakeule57-maker/mcp-memory [F api+README]: 0 stars, MIT, Python, pushed 2026-09-19. QUOTE: "A persistent memory for AI assistants that is just a SQLite file." QUOTE: "entries are filed on two axes — where it belongs (project) and what kind of knowledge it is". QUOTE: "Nothing is ever deleted — superseded entries drop out of the default view and stay findable" with "a note on why and by what". On embeddings: QUOTE: "it is a vector database that answers that something matched but never why". https://github.com/komakeule57-maker/mcp-memory
- Gentleman-Programming/engram [F api+README]: 6,795 stars, MIT, Go, pushed 2026-09-24, NOT a fork (parent of the many forks). QUOTE: "one binary, one SQLite file" (~/.engram/engram.db, FTS5). Tools mem_search / mem_timeline / mem_get_observation. QUOTE: "exposed through CLI, HTTP API, MCP, and an interactive TUI". QUOTE: "Git Sync exports portable compressed chunks for sharing across machines"; Obsidian export beta. Decay/graph/encryption NOT MENTIONED. Library use NOT MENTIONED. https://github.com/Gentleman-Programming/engram
- sdsrss/claude-mem-lite [F README via fork mekineer-com (fork of sdsrss)]: JS, MIT. QUOTE: "FTS5 BM25 scoring, an AND->OR rescue pass, pseudo-relevance feedback and concept co-occurrence." (TF-IDF arm "measured as net-negative and removed in v6.0.0"). QUOTE: "Type-differentiated half-lives (decisions: 90d, discoveries: 60d, bugfixes: 14d, changes: 7d) consistently applied in all ranking paths." 7 Claude Code hooks incl. PreCompact. LLM (Haiku / `claude -p`) used for background summarization => LLM-optional-ish but extraction needs it. QUOTE: "`mem_export` tool exports observations as JSON or JSONL". https://github.com/sdsrss/claude-mem-lite
- sqliteai/sqlite-memory [F api+README]: 120 stars, licence NOASSERTION on API / README says "MIT License", C, pushed 2026-07-21. QUOTE: "Your entire knowledge base lives in one portable `.db` file"; QUOTE: "Combines vector similarity (cosine distance) with FTS5 full-text search"; weights vector_weight/text_weight/search_oversample; QUOTE: "Local embedding with llama.cpp (no internet required)"; QUOTE: "Offline-first CRDT sync across devices" (sqlite-sync, block-level LWW). Deps: sqlite-vector, md4c, llama.cpp => C extension, CGo. Go CLI `sqlmem` with MCP. https://github.com/sqliteai/sqlite-memory
- frank123452/agent-memory [F api+README]: 0 stars, MIT, TypeScript, pushed 2026-09-18. QUOTE: "BM25 over character n-grams. No vector database, no embedding API, no network call." QUOTE: "Events are rules, not a classifier." QUOTE: "Non-pinned entries leave the injected block after `injectWindowDays` and are dropped entirely after `entryTtlDays`." QUOTE: "When history is compressed, a system message states how many turns were omitted, even when retrieval found nothing." https://github.com/frank123452/agent-memory
- GibsonAI/memori [F README]: Apache-2.0; positioned enterprise/cloud: QUOTE: "Memori Cloud — Zero config. Get an API key"; extraction from "agent execution and conversation" (LLM-driven; SQL-native). => off-thesis (LLM in store + cloud-first pivot). https://github.com/GibsonAI/memori
- k8nstantin/go-leiden [F api]: 4 stars, MIT, Go, created 2026-05-12, pushed 2026-05-12 (single-day repo). "Port of graspologic-native (Microsoft Research). Zero external dependencies." Young/unproven. https://github.com/k8nstantin/go-leiden
- ekzhu/minhash-lsh [F api]: 27 stars, MIT, Go, pushed 2019-09-24 (dormant). https://github.com/ekzhu/minhash-lsh
- mfonda/simhash [F api]: 233 stars, MIT, Go, pushed 2015-10-07 (dormant, but tiny/stable). https://github.com/mfonda/simhash
- hyperjumptech/grule-rule-engine [F api]: 2,532 stars, licence NOASSERTION (API), Go, pushed 2026-02-10. https://github.com/hyperjumptech/grule-rule-engine
- expr-lang/expr [F api]: 8,029 stars, MIT, Go, pushed 2026-07-07. https://github.com/expr-lang/expr
- google/cel-go [F api]: Apache-2.0, Go, pushed 2026-09-10 (stars returned 0 in fetch — UNVERIFIED count). https://github.com/google/cel-go
- ACT-R libs [S]: Python only — CarletonCognitiveModelingLab/python_actr, jakdot/pyactr, PyACTUp (base-level + spreading activation). No Go ACT-R lib found. Ghost already has its own ACT-R measured NO-GO (memory note).
- D discovery [S]: warrentc3/Agent-Memory-OS ("One SQLite file, no LLM required. Apache-2.0" ACL + federated sync), bozbuilds/AIngram ("sqlite-vec/QJL two-pass vector + FTS5 ... Ed25519-signed entries"), qualixar/superlocalmemory ("governed, local-first memory control plane", arXiv:2608.08253), riponcm/projectmem (PROJECTMEM paper arXiv:2606.12329: "append-only, plain-text event log of typed events ... deterministically projects that log into compact, AI-readable summaries"; "deterministic pre-action gate"; "Memory-as-Governance").

### Batch 4 (D positioning + the two big pure-Go findings)
- **modernc.org/sqlite/vec** [F pkg.go.dev]: module v1.59.0 (published 2026-09-15) ships subpackage `vec` bundling "sqlite-vec, the vector search extension for SQLite" v0.1.9; `import _ "modernc.org/sqlite/vec"` "installs the extension with sqlite3_auto_extension so that every connection opened afterwards has it loaded." CGo-free (transpiled like the rest). sqlite-vec part is Apache-2.0 OR MIT ("a different license from the rest of this module"; module is BSD-3-Clause). Ghost go.mod is on modernc.org/sqlite v1.45.0 (needs bump). Local `go get` verification blocked by sandbox proxy TLS (x509 OSStatus -26276) — UNVERIFIED locally. https://pkg.go.dev/modernc.org/sqlite/vec
- **gosqlite.org/vfs/crypto** [F pkg.go.dev]: v0.14.0 (2026-07-18), Apache-2.0. "Encrypts the main database file, rollback journal, WAL frames, temp DB, and sub/temp journals." Adiantum default (32-byte key) or AES-XTS-256; `DeriveKey()` Argon2id; "Pure Go via lukechampine.com/adiantum"; works with modernc.org/sqlite: "Wrap the default OS VFS with New(), pass the returned name into the DSN via `?vfs=<name>`." https://pkg.go.dev/gosqlite.org/vfs/crypto
- gosqlite.com [F]: QUOTE: "A pure-Go, drop-in replacement for `mattn/go-sqlite3`, `modernc.org/sqlite`, and the `glebarez/sqlite` gorm dialector"; QUOTE: "CGo-free. AI-ready. One module — driver, gorm dialector, vector & full-text search, rank fusion, encryption, compression, a writable custom VFS"; "Typed vec.Table over sqlite-vec: L2 / Cosine / Dot / Hamming, quantized encodings, streaming KNN"; "© 2026 The gosqlite Authors", Apache-2.0. Maintainer identity/stars NOT FOUND (no GitHub repo located). https://gosqlite.com/
- modernc.org/sqlite root [F pkg.go.dev]: v1.59.0 2026-09-15, BSD-3-Clause; subpackages vtab, vfs (read-only fs.FS VFS). Encryption NOT MENTIONED (search snippet S: "no SQLCipher support ... no encryption API surface"). https://pkg.go.dev/modernc.org/sqlite
- tursogo [F pkg.go.dev]: QUOTE: "No CGO: This driver uses the awesome purego library to call C (in this case Rust with C ABI) functions from Go." => dlopen of a Rust shared lib, not a single static Go binary. MIT. https://pkg.go.dev/turso.tech/database/tursogo
- dolthub/dolt README [F]: QUOTE: "Dolt has a dependency on [cgo](https://pkg.go.dev/cmd/cgo), so you will need a working C compiler and toolchain as well." QUOTE: "Dolt is a single ~103 megabyte program." => CGo + huge. https://github.com/dolthub/dolt
- yamantaka520/Agent-Memory-OS [F api+README of fork]: 5 stars, Apache-2.0, Python, created 2026-07-10, pushed 2026-09-22 (original; warrentc3 is a fork). QUOTE: "one SQLite file (FTS5), no server required"; QUOTE: "associative recall (resonance) — an authoritative memory_links graph lets related memories surface even when they share no query terms"; QUOTE: "exponential/linear decay, pinning, hard expiry, and a write-side consolidate() pass"; QUOTE: "Private / agent / team / project / global — hard gate before ranking"; federation QUOTE: "converge with last-writer-wins + tombstones"; 12 MCP tools. Audit/export/encryption/latency NOT MENTIONED. https://github.com/yamantaka520/Agent-Memory-OS
- bozbuilds/AIngram [F api+README]: 12 stars, Apache-2.0, Python, created 2026-03-28, pushed 2026-04-27 (stalled 5 months). QUOTE: "Your entire agent memory — entries, embeddings, entity graph, signing chain — lives in a single SQLite file." QUOTE: "Every memory entry is Ed25519-signed and linked in a tamper-evident hash chain. You can verify that a memory hasn't been modified since it was written." QUOTE: "Decay — reduces the importance score of memories that haven't been accessed recently (always active, no configuration needed)." QUOTE: "Multi-hop queries resolved via CTE." QUOTE: "22ms median retrieval latency — pure local pipeline, no network round-trip." Export/import/merge ("skips duplicates"). https://github.com/bozbuilds/AIngram
- qualixar/superlocalmemory [F api+README]: 226 stars, AGPL-3.0 (+commercial), Python, pushed 2026-09-12. QUOTE: "SQLite and sqlite-vec remain the canonical source of truth." QUOTE: "Current recall has five candidate producers—dense semantic, BM25 lexical, temporal, Hopfield associative, and spreading activation—followed by fusion, optional reranking, and entity-graph score enhancement." QUOTE: "Mode A can run core memory operations without sending memory content to a cloud model provider." QUOTE: "There is no retention timer counting down against a memory: what moves it outward is being left alone, and what pulls it back is being used." QUOTE: "Every store, recall, mutation, and erasure produces a hash-chained audit record." QUOTE: "SLM publishes no universal p99 claim." https://github.com/qualixar/superlocalmemory
- riponcm/projectmem [F api+README]: 835 stars, MIT, Python, pushed 2026-09-15. QUOTE: "memory lives in a plain `.projectmem/` directory inside your repository" append-only `events.jsonl`; QUOTE: "tools distill the raw log into compact AI-readable summaries"; QUOTE: "a pre-commit warning that fires _before_ you repeat an approach that already failed"; QUOTE: "`pjm init` installs three git hooks (pre-commit warnings, post-commit classification, post-merge tracking)"; latency QUOTE: "Latency is now flat rather than linear in project age" ("~33 s, 1,501 git processes" -> "82 ms, 2 processes"). 17 MCP tools. https://github.com/riponcm/projectmem
- sachinsharma9780/memweave [F README]: MIT, Python; markdown files + single SQLite index (FTS5 + sqlite-vec); embeddings via LiteLLM providers (Ollama local possible) or keyword-only offline; "exponential temporal decay with configurable half-life (default 30 days). Evergreen files are exempt". MCP NOT MENTIONED. https://github.com/sachinsharma9780/memweave
- go-leiden README [F]: `leiden.Leiden(ctx, n, edges, opts)`, weighted edges, `Resolution`, QUOTE: "Seed: 42, // 0 = random; >0 = fully deterministic", "unit + benchmarks + fuzz, karate-club modularity in [0.35, 0.45]", 10k-node benchmarks. https://github.com/k8nstantin/go-leiden
- grule README [F]: Apache-2.0; GRL `rule ... salience 10 { when ... then ... }`. https://github.com/hyperjumptech/grule-rule-engine
- litestream README [F]: QUOTE: "safely replicates changes incrementally to another file or S3"; backends dirs gs/oss/nats/sftp/webdav; Apache-2.0; embedding-as-library NOT MENTIONED on README. https://github.com/benbjohnson/litestream
- LangGraph SqliteSaver: docs pages redirected/empty — NOT VERIFIED (S only: checkpointer persists thread state per super-step; LLM-free store).

---
# SYNTHESIS (2026-09-24)

## A. LLM-free / LLM-optional agent memory stores — what each does that ghost doesn't
| Tool | Store / owned file | Retrieval | Lifecycle | Graph | Plug-in | Audit/history | Export | Enc | Lic / activity | Ghost lacks |
|---|---|---|---|---|---|---|---|---|---|---|
| Engram (Gentleman-Programming) [F] | one SQLite (~/.engram/engram.db) FTS5, Go | FTS5 | none stated | none | CLI/HTTP/MCP/TUI, per-agent setup cmds | mem_timeline | Git Sync compressed chunks, Obsidian beta | none | MIT, 6.8k★, pushed today | TUI + git-sync export; nothing algorithmic |
| LongMemory (CaviraOSS) [F] | SQLite, TS | gated recall (temporal/contradiction/contract/confidence/grounding) + associative | "deterministic decay, explicit reinforcement, consolidation, compression, reconsolidation" (formula unpublished) | "Hydrograph ... immutable nodes, executable edges, worlds, entities, facets, traces" | npm/CLI/HTTP/MCP/dashboard/VS Code | "immutable content, provenance, temporal truth" | import SQLite/JSON/JSONL | none | Apache-2.0, 4.5k★, active | **recall gates as named policy stages**; "historical recall preserves superseded truth" as a distinct mode |
| GoodMemory (hjqcan) [F] | SQLite (Bun), TS | "rules-only" single-pass, BM25 + entity adjacency | none | entity adjacency | MCP/HTTP/lib + managed Codex/Claude hooks | **audit ledger** (candidate previews, reasons, host, mode, ts) | exportMemory() pages/ + SHA-256 manifest | none | MIT, 18★, active | **governed writeback** (off/observe/review/selective) + assistant-originated memory blocked until host confirms |
| fidelis (hermes-labs) [F] | local (~/.cogito), Py | BM25 + dense (Ollama nomic) + RRF, verbatim passages | none | none | 6 MCP tools incl. fidelis_correct | corrections retain superseded; validity dates | none | none | MIT, 23★, active | verbatim-only contract; LongMemEval-S harness run |
| Basic Memory [F] | Markdown files + SQLite index, Py | FTS+FastEmbed hybrid, optional cross-encoder | none | Observations/Relations syntax, memory:// URLs | MCP; Obsidian | git (implied) | md is the export | none | AGPL-3.0, 4.0k★, active, $15/mo cloud | human-editable source of truth |
| komakeule57-maker/mcp-memory [F] | one SQLite FTS5, Py | FTS5 w/ German morphology | never delete; superseded hidden "with a note on why and by what" | none | MCP (only dep) | supersede note | none | none | MIT, 0★ | explicit anti-embedding stance ("answers that something matched but never why") |
| claude-mem-lite (sdsrss) [F] | one SQLite, JS | FTS5 BM25 + AND->OR rescue + PRF + co-occurrence (TF-IDF removed as net-negative) | **type-differentiated half-lives** 90/60/14/7 d | bidirectional obs links | 7 Claude Code hooks incl. PreCompact | atomic writes | mem_export JSON/JSONL | none | MIT (fork chain) | per-kind half-life; PreCompact hook capture |
| sqliteai/sqlite-memory [F] | one .db, markdown SoT, C ext | cosine + FTS5 weighted (vector_weight/text_weight/oversample) | none | none | SQL + Go CLI sqlmem MCP | none | md | none | MIT, 120★, C/llama.cpp (CGo) | **CRDT sync between agents** (sqlite-sync block LWW) |
| Agent-Memory-OS (yamantaka520) [F] | one SQLite FTS5, Py | FTS5 + "resonance" via memory_links | exp/linear decay, pinning, hard expiry, write-side consolidate() | memory_links | 12 MCP tools | none | none | none | Apache-2.0, 5★ | **ACL hard-gate before ranking**; federation LWW+tombstones |
| AIngram (bozbuilds) [F] | one SQLite (sqlite-vec+FTS5), Py | two-pass QJL vector + FTS5 | access-based importance decay | entity graph, CTE multi-hop | MCP | **Ed25519-signed hash chain** | export/import/merge | none | Apache-2.0, 12★, stalled since 2026-04 | tamper-evident entries; 22 ms median claim |
| superlocalmemory (qualixar) [F] | SQLite + sqlite-vec, Py | 5 producers (dense, BM25, temporal, Hopfield, spreading activation) + fusion | use-driven, no timers | entity graph | MCP http/stdio + hooks | **hash-chained audit of every op** | ? | none | AGPL-3.0, 226★ | audit-chain; explicit "no p99 claim" honesty |
| frank123452/agent-memory [F] | pluggable read/write, TS | BM25 char n-grams (CJK) | injectWindowDays / entryTtlDays, pinned exempt | none | lib primitives | none | none | none | MIT, 0★ | compression announces omitted turns to the model |
| projectmem (riponcm) [F] | .projectmem/events.jsonl append-only in repo, Py | deterministic projection | none | none | 17 MCP tools + 3 git hooks | the log IS the audit | CLAUDE.md/.cursorrules | none | MIT, 835★ | **pre-action gate** ("Memory-as-Governance"); flat latency 82 ms |
| memweave [F] | md files + SQLite (FTS5+sqlite-vec), Py | hybrid, keyword-only offline | exp decay half-life 30d, evergreen exempt | none | lib | none | md | none | MIT | — |
| Memori (GibsonAI) [F] | SQL, Py | LLM extraction | — | — | SDK | — | — | — | Apache-2.0; cloud-first pivot | OFF-THESIS |
Class note: dozens of near-identical "local-memory-mcp" SQLite+FTS5 clones (vheins, chriswessells, Beledarian, studiomeyer, eldarj, Nithin00614/localmem-mcp) [S] — nothing beyond FTS5 + optional local embeddings.

## B. Embedded infra mapped to ghost gaps
| Tech | Primitive | Closes | Pure-Go/no-CGo? | Lic | Cost |
|---|---|---|---|---|---|
| modernc.org/sqlite/vec (v1.59.0) [F] | sqlite-vec 0.1.9 vec0 tables in-process | **vector index gap** | YES (same driver ghost uses; bump v1.45→v1.59) | BSD-3 + Apache/MIT | tiny; sqlite-vec is brute-force KNN today (IVF/DiskANN dirs exist, pre-v1) — gives SIMD/quantized scan, not ANN |
| gosqlite.org/vfs/crypto v0.14.0 [F] | Adiantum/AES-XTS page-level VFS for modernc | **encryption at rest** ("file vault") | YES | Apache-2.0 | one dep + key mgmt; deterministic cipher, no forgery protection |
| ncruces/go-sqlite3 + sqlite-vec-go-bindings/ncruces [F] | wazero wasm build w/ sqlite-vec; vfs/adiantum | same two gaps | YES (wasm runtime) | MIT | driver swap + wazero; ~ only if modernc route fails |
| gosqlite.com [F] | "one module — driver, vec, fts, rank fusion, encryption, compression, writable VFS" | both + RRF | claims yes | Apache-2.0 | maintainer unknown, no repo found — UNVERIFIED provenance |
| Dolt + dolthub/driver [F] | AS OF, dolt_history_<t> (row per revision), dolt_diff | per-cell audit model | NO (CGo, ~103 MB) | Apache-2.0 | prohibitive; steal the *query shape* only |
| XTDB v2 [F] | SQL:2011 bitemporal "ubiquitous and automatic" | as-of API design | NO (JVM/Docker) | MPL | reference only |
| CozoDB [F] | Datalog rules + HNSW + time travel | rules-as-queries model | NO (Rust, Go via C API) ; no commit since 2024-12 | MPL-2.0 | dormant; design reference only |
| Kùzu [F] | embedded Cypher graph | — | NO; **archived 2025-10** | MIT | DEAD |
| DuckDB go-duckdb [F] | analytics | — | NO (CGo, static lib) | MIT | off |
| LanceDB [F] | Lance format, auto versioning | — | NO (no Go SDK) | Apache-2.0 | off |
| Turso (tursogo) [F] | native vectors (exact), experimental encryption, SQLite-file compatible | both gaps | "No CGO" but purego dlopen of Rust lib | MIT | not single-static-binary; pre-1.0 |
| Litestream [F] | incremental WAL replication to file/S3/etc | user-file backup/sync | YES (Go) | Apache-2.0 | sidecar; DB stays owned |
| cr-sqlite [F] | CRDT multi-writer SQLite | multi-agent sync | NO (Rust ext) | MIT | off unless wasm |

## C. Algorithm libraries (Go)
- go-fsrs (open-spaced-repetition) [F]: FSRS v6, 21 weights, Card lifecycle, MIT, 146★, pushed 2026-07. Maps onto tier promotion: stability/difficulty per memory, `Next(card, now, grade)` where grade = retrieval outcome.
- gonum graph/community [F]: Louvain `Modularize(g, resolution)`, BSD-3, maintained. Fixes the hairball cluster (memory note) cheaply.
- k8nstantin/go-leiden [F]: Leiden, weighted, `Seed>0 = fully deterministic`, 4★, single-day repo (2026-05) — unproven; vsuryav/leiden-go, GoGraph [S] alternatives.
- MinHash/SimHash: ekzhu/minhash-lsh (2019), mfonda/simhash (2015) [F] — dormant but tiny; vendor rather than depend.
- Rules: expr-lang/expr (8.0k★, MIT, 2026-07) [F] and google/cel-go (Apache-2.0, 2026-09) [F] — expression evaluators fit ghost's declarative rules table; grule (Apache-2.0, GRL when/then/salience, pushed 2026-02) [F] is a full RETE-style engine, heavier.
- ACT-R: Python only (python_actr, pyactr, PyACTUp) [S]; no Go lib. Hebbian: none found.

## D. Memory-as-infra positioning (local-first, LLM-free)
- Engram: "one binary, one SQLite file" + CLI/HTTP/MCP/TUI; no latency claim.
- LongMemory: "Your model stays stateless"; engine across npm/CLI/HTTP/MCP; no latency numbers.
- GoodMemory: "owns memory storage, recall, context assembly, write governance, and audit/export/delete"; writeback modes as the knob.
- Agent-Memory-OS: decay mode / pinning / hard expiry / consolidate() knobs; ACL gate.
- AIngram: "22ms median retrieval latency".
- projectmem: "Latency is now flat rather than linear in project age" (82 ms).
- superlocalmemory: "publishes no universal p99 claim".
- LangGraph checkpointer: state-store framing, docs unreachable — UNVERIFIED.

## (1) Closes a ghost gap — item → gap → cost
1. modernc.org/sqlite/vec → no vector index → bump modernc v1.45→v1.59, blank import; sqlite-vec pre-v1 (breaking changes), still exact KNN but SIMD/quantized; licence notice.
2. gosqlite.org/vfs/crypto → no encryption at rest → +1 pure-Go dep (lukechampine adiantum), `?vfs=` DSN, key derivation UX; no tamper protection.
3. go-fsrs → tier/importance decay is ad-hoc → +1 small dep; map retrieval outcomes to grades; optimizer not ready.
4. gonum community Louvain (then go-leiden if needed) → hairball clusters → gonum dep (large but pure Go).
5. Steal designs (no dep): GoodMemory writeback modes + audit ledger schema; superlocalmemory/AIngram hash-chained audit; projectmem pre-action gate; Dolt `dolt_history`/AS OF query shape over ghost's versions+bitemporal columns; LongMemory named recall gates; claude-mem-lite per-kind half-lives; Agent-Memory-OS ACL hard-gate; komakeule "superseded stays findable with why/by-what".
6. Litestream → user-file backup/sync → sidecar, zero code.
7. expr / cel-go → rules table needs richer predicates → +1 dep; deterministic, sandboxed.

## (2) Tempting but off-thesis
- Kùzu (archived 2025-10), CozoDB (dormant since 2024-12, CGo), Dolt (CGo, 103 MB), DuckDB (CGo), LanceDB (no Go), XTDB (JVM), cr-sqlite/sqlite-memory/sqliteai (C exts, llama.cpp), Turso (purego dlopen, pre-1.0).
- Memori (LLM extraction + cloud pivot), claude-mem (LLM compression, Chroma), Basic Memory (AGPL + cloud), superlocalmemory (AGPL + commercial), AIngram (stalled).
- gosqlite.com (unverifiable maintainer/repo).
- ACT-R libs (Python only; ghost measured NO-GO).

---
# CORRECTIONS after review (supersede the lines above where they conflict)

- **gosqlite.org/vfs/crypto is NOT a drop-in for modernc.** [F pkg.go.dev imports] It imports `gosqlite.org` (the driver) and `gosqlite.org/internal/cabi`, plus `modernc.org/sqlite/lib`, `modernc.org/libc`, `lukechampine.com/adiantum`, `golang.org/x/crypto/{argon2,xts}`. gosqlite.org = **github.com/go-again/sqlite** [F api]: 12 stars, Apache-2.0, Go, created 2026-05-26, pushed 2026-07-18, 0 open issues. Doc QUOTE: "a drop-in replacement for both github.com/mattn/go-sqlite3...and modernc.org/sqlite (the upstream CGo-free wrapper this module is built on top of)". So encryption cost = **swap driver to a 4-month-old, 12-star module layered on modernc** — OR use ncruces/go-sqlite3 vfs/adiantum (1.1k stars, mature) which also requires a driver swap (wazero). Either way encryption-at-rest = driver swap, not "+1 dep". List (1) #2 rewritten below; gosqlite.com listed in (2) as "young/unproven", not "unverifiable" (repo now found).
- **modernc.org/sqlite/vec confirmed** [F pkg.go.dev versions+imports]: present since **v1.47.0 (2026-03-17)** through v1.59.0; imports only `modernc.org/libc`, `modernc.org/sqlite/lib`, reflect, unsafe — i.e. ccgo-transpiled, no cgo. Correction to cost line: transpiled C has **no SIMD**; the real gain is vec0 storage, int8/binary quantization and a maintained KNN path (still exact scan, not ANN). sqlite-vec upstream last pushed 2026-05-18, pre-v1.
- localmem-mcp [F]: parent is OpenAgentHQ/localmem-mcp (7 stars per fork note), MIT, Python. QUOTE: "Memories live in one SQLite file you own"; QUOTE: "embeddings are computed on-device with fastembed"; QUOTE: "every memory is scored by cosine similarity against the query embedding, and memories that also hit the FTS5 keyword index get a bounded bonus"; `localmem-mcp export > memories.jsonl`. No lifecycle. Nothing ghost lacks.
- thedotmack/claude-mem [F api+README]: 94,598 stars, Apache-2.0, TS, pushed 2026-09-24. QUOTE: "5 Lifecycle Hooks - SessionStart, UserPromptSubmit, PostToolUse, Stop, SessionEnd"; SQLite + QUOTE: "Chroma Vector Database"; summaries generated by an AI provider (claude-mem observer/OpenRouter/Gemini/Anthropic) => LLM in the store => list (2). Its hook set is the canonical "harness plugs in" pattern for A.

## (1) Closes a ghost gap — FINAL
1. modernc.org/sqlite/vec (≥v1.47.0) → no vector index → bump modernc v1.45→v1.59 + blank import; CGo-free confirmed via imports; gain = vec0 tables + quantization, NOT ANN/SIMD; sqlite-vec pre-v1 and quiet since 2026-05; Apache/MIT notice.
2. Encryption at rest → driver swap required: (a) ncruces/go-sqlite3 vfs/adiantum (MIT, 1.1k★, wazero runtime; sqlite-vec via sqlite-vec-go-bindings/ncruces) or (b) gosqlite.org (go-again/sqlite, 12★, 2026-05, Apache-2.0, vfs/crypto Adiantum/AES-XTS + bundled sqlite-vec + rank fusion, layered on modernc). Cost: driver migration + key UX; neither protects against tampering.
3. go-fsrs → principled decay/promotion → +1 small dep (MIT, v6, 21 weights).
4. gonum graph/community Louvain (go-leiden later) → hairball clusters → gonum dep.
5. Design steals (no dep): GoodMemory writeback modes + audit ledger; superlocalmemory/AIngram hash-chained audit; projectmem pre-action gate; Dolt dolt_history/AS OF query shape; LongMemory named recall gates; claude-mem-lite per-kind half-lives; Agent-Memory-OS ACL hard-gate; komakeule "superseded stays findable with why/by-what"; claude-mem 5-hook capture surface.
6. Litestream → backup/sync of the owned file → sidecar.
7. expr / cel-go → richer rule predicates → +1 dep.

## (2) Tempting but off-thesis — FINAL
Kùzu (archived 2025-10) · CozoDB (dormant since 2024-12, CGo) · Dolt (CGo, ~103 MB) · DuckDB (CGo) · LanceDB (no Go SDK) · XTDB (JVM) · cr-sqlite, sqliteai/sqlite-memory (C ext + llama.cpp) · Turso tursogo (purego dlopen, pre-1.0) · Memori (LLM extraction, cloud pivot) · claude-mem (LLM summaries + Chroma) · Basic Memory (AGPL + cloud) · superlocalmemory (AGPL/commercial) · AIngram (stalled 2026-04) · gosqlite.org as a *driver* bet (12★, 4 months old) · ACT-R libs (Python only; ghost measured NO-GO).
